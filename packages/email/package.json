{
  "name": "@swe-quant/email",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@swe-quant/config": "workspace:*",
    "@swe-quant/shared": "workspace:*",
    "nodemailer": "^9.0.5",
    "resend": "^4.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.8.3"
  }
}
